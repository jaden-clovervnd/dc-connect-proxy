{
  "name": "dc-connect-proxy",
  "version": "0.1.0",
  "description": "CORS/Referer-bypass proxy + parser for dcinside, feeding the Even Realities G2 Even Hub app",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "smoke": "node smoke.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.19.2"
  }
}
